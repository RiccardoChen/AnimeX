spring:
  application:
    name: Animex_back-end
  datasource:
    url: jdbc:mysql://localhost:3306/animex?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none
  banner:
    location: classpath:banner.txt
jwt.secretKey: ThisIsMySecretKeyIllNotToTellYou